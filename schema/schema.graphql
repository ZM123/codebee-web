type Mutation {
  hello: String!
  createUser(input: UserInput): User
  updateUser(id: Int!, input: UserInput): User
}

type Query {
  hello: String!
  user(id: Int!): User
  users: [User]!
}

type User {
  id: Int!
  name: String!
}

input UserInput {
  name: String!
}
