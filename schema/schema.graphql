type Mutation {
  hello: String!
  createUser(input: UserInput): User
  updateUser(id: ID!, input: UserInput): User
}

type Query {
  hello: String!
  user(id: ID!): User
  users: [User]!
}

type User {
  id: ID!
  name: String!
}

input UserInput {
  name: String!
}
